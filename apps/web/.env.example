# Forever web (landing)

Optional waitlist backends — set one:

# POST JSON { "email": "..." } to your own endpoint
VITE_WAITLIST_ENDPOINT=

# Or Formspree form id
VITE_FORMSPREE_ID=

# If neither is set, emails are stored in the browser localStorage for local demos.
